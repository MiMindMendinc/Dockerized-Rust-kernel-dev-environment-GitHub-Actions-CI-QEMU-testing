[package]
name = "mimindmend-rust-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
bootloader = "0.9"
serde = { version = "1", default-features = false }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"